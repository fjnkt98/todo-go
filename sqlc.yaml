version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    database:
      uri: ${DATABASE_URL}
    gen:
      go:
        out: "repository"
        emit_pointers_for_null_types: true
        emit_db_tags: true
